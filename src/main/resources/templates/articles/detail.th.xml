<?xml version="1.0" ?>
<thlogic>
    <attr sel="#header" th:replace="header :: header"></attr>
    <attr sel="#footer" th:replace="footer :: footer"></attr>

    <attr sel="#article-main" th:object="${article}">
        <attr sel="#article-header/h1" th:text="*{title}"/>
        <attr sel="#nickname" th:text="*{nickname}"/>
        <attr sel="#email" th:text="*{email}"/>
        <attr sel="#create-at" th:text="*{#temporals.format(createAt, 'yyyy-MM-dd')}" th:datetime="*{createAt}" />
        <attr sel="#hashtag" th:text="*{hashtag}"/>
        <attr sel="#article-content/pre" th:text="*{content}"/>

        <attr sel="#delete-article-form" th:action="'/articles/' + *{id} + '/delete'" th:method="post">
            <attr sel="#update-article" th:href="'/articles/' + *{id} + '/form'"/>
        </attr>

        <attr sel=".article-id" th:name="articleId" th:value="*{id}"/>
        <attr sel="#comment-form" th:action="@{/comments/new}" th:method="post">
            <attr sel="#comment-textbox" th:name="content"/>
        </attr>

        <attr sel="#article-comments" th:remove="all-but-first">
            <attr sel="li[0]" th:each="articleComment : ${articleComments}">
                <attr sel="form" th:action="'/comments/' + *{id} + '/' + ${articleComment.id} + '/delete'" th:method="post">
                    <attr sel="div/strong" th:text="${articleComment.nickname}"/>
                    <attr sel="div/small/time" th:datetime="${articleComment.createAt}" th:text="${#temporals.format(articleComment.createAt, 'yyyy-MM-dd')}"/>
                    <attr sel="div/p" th:text="${articleComment.content}"/>
                </attr>
            </attr>
        </attr>

        <attr sel="#pagination">
            <attr sel="ul/li[0]/a"
                  th:href="*{id} -1 <= 0 ? '#' : |/articles/*{id-1}|"
                  th:class="'page-link' + (*{id} -1 <= 0 ? ' disabled' : '')"
            />
            <attr sel="ul/li[1]/a"
                  th:href="*{id} +1 >= ${articleTotalCount} ? '#' : |/articles/*{id+1}|"
                  th:class="'page-link' + (*{id} +1 >= ${articleTotalCount} ? ' disabled' : '')"
            />
        </attr>
    </attr>
</thlogic>